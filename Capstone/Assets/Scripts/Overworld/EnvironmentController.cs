using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnvironmentController : MonoBehaviour
{
    //Contains lists of all the environment objects that can be interacted with using the overworld rhythm

    public EnvironmentNote[] interactables; //List of all interactable note blocks. 

    //I regret not using abstracts


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
