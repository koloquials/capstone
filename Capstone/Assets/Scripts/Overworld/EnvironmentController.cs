using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnvironmentController : MonoBehaviour
{
    //Contains lists of all the environment objects that can be interacted with using the overworld rhythm

    public NoteLock[] lockList; //List of all locks
    public NoteBlock[] blockList; //List of all blocks

    //List of all individual note blocks


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
